module.exports = {
name:"kick",
code:`
$description[$username[$authorID] kicked $username[$findUser[$message[1]] for $filterMessage[$message;$message[1]]
$kick[$findUser[$message[1]];$filterMessage[$message;$message[1]]]`
`  
}
